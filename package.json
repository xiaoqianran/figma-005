{
  "name": "watch-me-2026",
  "version": "1.0.0",
  "description": "Watch Me 2026 — Premium luxury smart instrument. Complete redesign: cinematic brand landing + production-grade interactive smartwatch OS simulator + high-fidelity modular UI kit. Zero-dependency, self-contained, Figma-faithful.",
  "private": true,
  "scripts": {
    "start": "npx http-server -p 8080 -o index.html -c-1",
    "serve": "npx http-server -p 3000 -c-1 --cors",
    "preview": "npx live-server --port=8080 --open=/index.html 2>/dev/null || npx http-server -p 8080 -o index.html -c-1",
    "dev": "npx http-server -p 5173 -c-1 --cors",
    "verify": "node scripts/verify.js",
    "test": "node scripts/test-os.mjs",
    "test:os": "node scripts/test-os.mjs",
    "launch:smoke": "node scripts/launch-smoke.mjs",
    "lint:html": "npx html-validate index.html prototypes/*.html components/**/*.html 2>/dev/null || echo 'html-validate not installed or no issues reported'",
    "build:info": "echo 'Watch Me 2026 - static artifacts ready. No build step required for core deliverables.'"
  },
  "keywords": [
    "watch-me",
    "smartwatch",
    "luxury-ui",
    "figma-replica",
    "interactive-prototype",
    "ui-kit",
    "dark-theme",
    "vanilla-js",
    "tailwind",
    "pwa",
    "self-contained"
  ],
  "author": "Watch Me Atelier",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://example.local/watch-me/2026.git"
  },
  "engines": {
    "node": ">=18"
  }
}
